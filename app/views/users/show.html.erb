<p> 

<h1>Events Near Me</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th>Time</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @near_me.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.location %></td>
        <td><%= event.start_time %></td>
        <td><%= truncate(strip_tags(event.description), length: 120) %></td>
        <td><%= link_to 'Show', event %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>


<br />
<%= link_to "My Events", action: "index", controller: "events" %>

</p>

